import React from 'react';
import { Button } from 'antd';
import { Icon } from '@components/Icon';

export const DevelopPage = () => {
  return (
    <section>
      <h1>开发页</h1>
      <Icon name="setting" size={48} />
    </section>
  );
};
